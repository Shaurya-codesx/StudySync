name: Ktor CI pipeline V2

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./server

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpassword
          POSTGRES_DB: studysync
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build and Test
        env:
          DATABASE_URL: jdbc:postgresql://localhost:5432/studysync?user=testuser&password=testpassword
          JWT_SECRET: ci-dummy-secret-key-123
          AI_API_KEY: mock_api_key
          SMTP_EMAIL: ci_dummy_email@gmail.com
          SMTP_PASSWORD: ci_dummy_password
        run: ./gradlew build --no-daemon